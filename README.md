# hotdoc

Hotdoc is a local-first, keyboard-driven desktop launcher that answers one question fast: "what's the exact syntax for the tool I already know?" Press a global hotkey, type a few fuzzy words, get the command, copy it, paste it, back to flow — target ≤ 5 seconds from hotkey to paste. No AI, no account, no subscription, no browser, no network.
