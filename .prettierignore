dist
build
release
node_modules
coverage
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock
pnpm-lock.yaml
*.min.js
*.min.css

# Hand-curated fixture data — formatter must not touch these
docs/seed-data/**

# Internal docs (PRD, spec, plans, handoffs) — hand-written, not CI-gated
docs/internal/**

# Editor config files (managed by the editor, not prettier)
.vscode/**
