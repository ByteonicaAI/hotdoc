// Placeholder so the [[bin]] target builds before T6 fills in the subcommands.
// T6 replaces this body with: index / query / copy / bench.
fn main() {
    eprintln!("hotdoc-core CLI: not yet implemented (T6)");
}
